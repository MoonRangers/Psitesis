import {AppBar, Toolbar, Typography} from '@material-ui/core'

function NavBar () {
    return (
        
        <AppBar position="top">
            <Toolbar>
                <Typography variant="h6" >
                    PsiTesis
                </Typography>
            </Toolbar>
        </AppBar>
    )
}

export default NavBar;
